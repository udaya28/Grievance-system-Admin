export const getComplaintsDetails = (data) => {
    let result = {
        totalComplaints:data.length,
        ResponseMade:0,
        notResponded:0,
    }
    
    result.ResponseMade++
  console.log(result);
  return 0;
};